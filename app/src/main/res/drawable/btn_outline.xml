<?xml version="1.0" encoding="utf-8"?>
<!-- Neutral OUTLINE secondary action (e.g. snooze "తర్వాత చెప్పు").
     Quiet: just a rounded brown border on cream, with a soft ripple. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/ripple_dark">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFFFF" />
            <corners android:radius="22dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/cream" />
            <stroke android:width="2dp" android:color="@color/brown" />
            <corners android:radius="22dp" />
        </shape>
    </item>
</ripple>
