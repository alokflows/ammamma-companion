<?xml version="1.0" encoding="utf-8"?>
<!-- Soft pill behind the home weather tile — same warm card tone as the face circles,
     so it reads as "part of the home", not an alert. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#F3E7D3" />
    <corners android:radius="18dp" />
</shape>
