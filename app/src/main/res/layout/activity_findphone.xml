<?xml version="1.0" encoding="utf-8"?>
<!-- WP-LUX: "here I am!" stop screen. Ink backdrop + a red, slowly-breathing
     ambient glow (urgency without panic) behind one centered luminous card. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ink_bg">

    <com.ammamma.companion.ui.GlowBackdrop
        android:id="@+id/glow"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:id="@+id/card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginHorizontal="20dp"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/card_lux"
        android:padding="32dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:includeFontPadding="true"
            android:text="ఇక్కడ ఉన్నా!"
            android:textColor="@color/ink_accent_red"
            android:textSize="46sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:includeFontPadding="true"
            android:text="Phone is here"
            android:textColor="@color/ink_text_dim"
            android:textSize="20sp" />

        <Button
            android:id="@+id/stop"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="48dp"
            android:minHeight="96dp"
            android:paddingTop="24dp"
            android:paddingBottom="24dp"
            android:background="@drawable/btn_primary_green"
            android:stateListAnimator="@null"
            android:elevation="0dp"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:text="ఆపు"
            android:textSize="34sp" />

    </LinearLayout>
</FrameLayout>
