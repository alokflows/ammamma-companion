<?xml version="1.0" encoding="utf-8"?>
<!-- One recordable line: Telugu label + "say this" hint, then big buttons.
     Play/Delete only appear once a clip exists; the row turns light green then
     (background swapped in code to card_clip_done). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="14dp"
    android:paddingVertical="10dp"
    android:background="@drawable/card_face">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/clipLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#402A1C"
            android:textSize="17sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/clipHint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="#8A7361"
            android:textSize="13sp" />
    </LinearLayout>

    <!-- ▶ listen to the saved clip — only when one exists -->
    <Button
        android:id="@+id/btnPlay"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/btn_outline"
        android:padding="0dp"
        android:text="▶"
        android:textColor="#402A1C"
        android:textSize="20sp" />

    <!-- ✕ remove the clip (falls back to TTS) — only when one exists -->
    <Button
        android:id="@+id/btnDelete"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/btn_outline"
        android:padding="0dp"
        android:text="✕"
        android:textColor="#C62828"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- 🎤 record; becomes a red ⏹ stop while recording -->
    <Button
        android:id="@+id/btnRecord"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/btn_primary_green"
        android:padding="0dp"
        android:text="🎤"
        android:textColor="#FFFFFF"
        android:textSize="20sp" />
</LinearLayout>
